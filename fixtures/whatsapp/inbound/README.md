# WhatsApp inbound fixtures (append-only, B4)

Synthesized from Meta's publicly documented Cloud API webhook shapes (developers.facebook.com/docs/whatsapp/cloud-api/webhooks).
**Not yet verified against live traffic** — flag any real-traffic discrepancy found later as a new,
additional fixture (never edit an existing one; see CLAUDE.md).

Each file is one raw `POST /webhook` body as Meta would send it.
