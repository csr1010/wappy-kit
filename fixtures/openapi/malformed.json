{
  "openapi": "3.0.3",
  "info": { "title": "Malformed spec", "version": "1.0.0" },
  "paths": {
    "/pets": {
      "get": {
        "operationId": "listPets",
        "responses": "this should be an object, not a string"
      }
    }
  }
}
