# M0 — Test harness, gates, guardrails

**Spec:** §2, §12 · **Depends on:** — · **Tag:** `m0-done`
**Goal:** Regressions become detectable *before* features exist. Everything later leans on this.

## Read / skip
Read: SPEC §2 (architecture), §12 (test strategy). Skip: everything else.

## Tasks
- [x] **T0.1** Monorepo scaffold: pnpm + turbo, 5 packages, eslint, tsconfig.base, CI (`pnpm verify`). *(commit 2a11f34)*
- [x] **T0.2** Rename existing placeholder tests to `index.m0.test.ts` (gate rejects untagged tests).
- [x] **T0.3** `scripts/gate.mjs` (cumulative gate, quiet output, B2 immutability check) and `scripts/ctx.mjs` (brief + spec-section extractor); root scripts `gate`, `ctx`.
- [ ] **T0.4** `packages/testkit` (private, `@wappy/testkit`): 
  - `tmpProject()` — temp dir, auto-cleaned, `HOME` isolated.
  - `fakeClock()` — injectable `Clock` (advance timers deterministically).
  - `signWebhook(body, secret)` — valid `X-Hub-Signature-256`.
  - `mockWhatsAppCloud()` — local HTTP server that records sends and can be scripted: fail-with-code N times, 429, 5xx, emit status webhooks (sent/delivered/read/failed).
  - `mockModel()` — scripted responses (text, structured output, tool calls, errors, context-length error), records every call/prompt.
  - `mockOpenApiServer(spec)` — serves the spec + implements endpoints from fixtures.
  - Each helper has its own self-test.
- [ ] **T0.5** `packages/e2e` (private): empty spine harness + one placeholder passing test so the package is wired into turbo.
- [ ] **T0.6** Architecture guard test (B9): parse every `package.json` + source imports; assert core → nothing, plugins → core only, no plugin↔plugin.
- [ ] **T0.7** Coverage ratchet (B8): `@vitest/coverage-v8`, `coverage-baseline.json`, `scripts/ratchet.mjs`; gate fails on drop.
- [ ] **T0.8** CI runs `pnpm gate 0` (later: `pnpm gate <latest>`); add Node 20/22/24 matrix.

## Tests introduced (`*.m0.test.ts`)
- testkit self-tests (each mock: records, scripts failures, cleans up).
- `arch.m0.test.ts` — architecture guard (also proves the guard fails on a planted violation in a temp fixture).
- `gate.m0.test.ts` — gate script: untagged test rejected; missing `m<n>` suites rejected; deleted old test detected (uses a temp git repo).

## Backward testing
Nothing earlier exists. This milestone *creates* the mechanisms (B1, B2, B8, B9) and self-tests them so later milestones can trust them.

## Exit gate
`pnpm gate 0` → green. Tag `m0-done`.

## Context notes
testkit is where token-heavy boilerplate lives — keep its public API tiny and documented in a header comment so later sessions never open its internals.
