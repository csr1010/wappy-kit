# M9 — create-wappy CLI

**Spec:** §4, §5, §10 (install/state), §12 (install DX) · **Depends on:** M2, M4–M8 · **Tag:** `m9-done` · **Package:** `create-wappy`
**Goal:** `npm create wappy` → short interview → a runnable project. Resume/status/reset/doctor/dev all work.

## Read / skip
Read: §4 (esp. 4.1 v2.3), §5, §10 install/state, §12 install DX. Skip: internals of other packages (use their public API only).

## Tasks
- [x] **T9.1** Interview as a **pure state machine** (SPEC §4.1 v2.3): only **model → tools (none/Shopify) → skills (asked only after Shopify)**. No credential is ever asked; framework/memory/router are fixed in v0.1 (not offered until implemented). Thin `@clack/prompts` layer on top.
- [x] **T9.2** Non-interactive mode: `--yes --model <m> --api <none|shopify> --skills <list>`; same code path as interactive; no credential flags.
- [x] **T9.3** Generators (idempotent, ledger-driven): `index.ts`, `tools/*.ts`, `skills/*.ts`, **`.env.sample`** (every key needed to run, grouped, where-to-find notes, optional keys commented out; includes `WHATSAPP_APP_SECRET`), `.gitignore` (`.env`, `.wappy/`), README (`cp .env.sample .env`, Meta-creds walkthrough), package.json.
- [ ] **T9.4** ~~Existing-framework adapter stubs~~ **DEFERRED to v0.2** (SPEC v2.3): the framework question was removed from the v0.1 interview (Vercel AI SDK is fixed; Mastra unused).
- [ ] **T9.5** Commands: `create-wappy` (run/resume) **done**; still to do: `wappy status`, `wappy reset [--plugin]`, `wappy doctor`, `wappy dev`.
- [ ] **T9.6** `doctor`: env present/filled, model key validity (cheap ping), WhatsApp creds (Graph API check), API reachability (SSRF-guarded), drift detection; each check has a clear fix hint. All network via injectable client (mocked in tests).
- [ ] **T9.7** `dev`: boot server (webhook route + GET handshake) + tunnel (provider pluggable; decision recorded — vendor-neutral, no account required preferred), print public `/webhook` URL + verify token.
- [ ] **T9.8** Snapshot each canonical combo's output to `fixtures/generated/m9-<combo>/` (B4/B7).

## Tests introduced (`*.m9.test.ts`, generated-project tests in e2e)
- Interview machine: table tests over all paths; invalid combos blocked; back/skip behavior.
- **Combo matrix** (every model × tools {none, Shopify+each skill subset}; v0.1 has no framework/memory/router/WhatsApp choices): each generates a project that **typechecks, boots, and answers Spine A** with mocks.
- **Resume-at-every-boundary:** run the CLI as a child process, `SIGINT`/`SIGKILL` after step k for every k; rerun → completes, no duplicated side effects, final tree == uninterrupted run.
- `reset` (all / per plugin) removes exactly the generated files; `doctor` catches missing/invalid key, bad creds, unreachable API, hand-edited file (drift).
- Secrets: canary key typed in the interview appears nowhere in generated files/state (grep test).
- Broken OpenAPI URL → interview fails at that step with a clear message, state records `failed`, resume works after fixing.

## Backward testing
M0–M8; **spine A/B/C now run through a *generated* project**; contract snapshot; B7 begins: `fixtures/generated/m9-*` must keep passing forever (later milestones that change templates add new fixtures, never edit these); `state.json` v-fixtures still load via the CLI.

## Exit gate
`pnpm gate 9` green; tag `m9-done`.

## Context notes
Templates are data — snapshot them, don't re-read them. Interview logic stays pure so most of this milestone is testable without a TTY.
