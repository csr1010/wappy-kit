# M2 — Install-state ledger

**Spec:** §5, §10 (install/state) · **Depends on:** M1 · **Tag:** `m2-done` · **Package:** `@wappy/core`
**Goal:** `.wappy/state.json` makes installs resumable, lockable, migratable, resettable.

## Read / skip
Read: §5, §10 install/state list. Skip: WhatsApp/tools specifics.

## Tasks
- [ ] **T2.1** State schema v1: `schemaVersion`, parts+versions, steps `{id,status,error?}`, `envKeys {name, required, filled}` (**names + booleans only — never values**), `lastStep`, timestamps, `runId`, generated-file hashes (for drift).
- [ ] **T2.2** Atomic read/write: write temp + fsync + rename; never leaves a half-written file.
- [ ] **T2.3** Lock file: pid + timestamp, stale-lock takeover, timeout, released on crash paths; `withLock(fn)`.
- [ ] **T2.4** Idempotent step runner `runStep(id, fn)`: skips `done`, records `failed` with error, resumable from first incomplete.
- [ ] **T2.5** Manifest aggregation: parts contribute `SetupManifest`; ledger = union; unknown/removed steps handled.
- [ ] **T2.6** Validation + corruption handling: typed `StateCorruptError`; `offerReset()` result (no interactive I/O in core).
- [ ] **T2.7** Migrations framework: `migrate(state)` chain; **newer** schemaVersion than supported → refuse with actionable message (no silent downgrade). Commit `fixtures/state/v1.json`.
- [ ] **T2.8** `reset(scope)` (all | plugin) returning the list of generated files to remove; `detectDrift()` comparing stored hashes.
- [ ] **T2.9** Core/part version skew check (peerDep range + runtime), returns warnings.

## Tests introduced (`*.m2.test.ts`)
- Crash mid-write (kill between temp write and rename) → old state intact.
- Concurrent runs: spawn 5 child processes racing `withLock`; exactly one wins at a time; stale lock recovered after simulated `kill -9`.
- Resume: fail step k of n, rerun → steps < k not re-executed (spy counters).
- Corrupt JSON / wrong types / truncated file → `StateCorruptError`, never a crash or silent reset.
- Env: filled values never appear in serialized state (grep test with a canary secret).
- Migration loop over **every** `fixtures/state/v*.json`; future-version fixture refused.
- Drift: edit generated file → detected; unchanged → clean.

## Backward testing
M0+M1 suites; contract snapshot (additions to core API only). B4 begins: `fixtures/state/v1.json` is permanent — every later schema bump adds `v2.json` and must still load v1.

## Exit gate
`pnpm gate 2` green; tag `m2-done`.

## Context notes
Lock/atomic-write code is the classic place for subtle bugs — keep it in one small module with the tests above as its spec.
