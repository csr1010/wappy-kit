# M8 — Shopify connector, RAG, reference skills

**Spec:** §8 (Shopify), §9 (Scenarios B, C), §15.4, §13 (boundary) · **Depends on:** M5–M7 · **Tag:** `m8-done` · **Packages:** `@wappy/tools-openapi`, `@wappy/harness`
**Goal:** Scenarios B (knowledge) and C (tool) run end-to-end, including the confirm-before-write round trip.

## Read / skip
Read: §8 Shopify bullet, §9 B and C, §15.4. Skip: CLI.

## Tasks
- [x] **T8.0** Verify Shopify's current API surface (Admin GraphQL vs REST; versioning; auth via access token) from official docs; record decision + version pin in PROGRESS. Do not rely on memory of the API.
- [x] **T8.1** Shopify connector = curated `ToolProvider`, read-only by default: products (search/get), orders (get by id/name, list recent), inventory levels, customers (lookup). Built on the generic engine where possible (§8: "engine stays generic"); token from env; API version configurable.
- [x] **T8.2** Connector response shaping: trim to fields the model needs (bounded by M6 caps); PII minimization documented (§11).
- [x] **T8.3** `Knowledge` module in harness (ratifies M1 decision): ingest local files/markdown/text, chunk, **lexical (BM25) index by default**, optional embedding index via the configured model provider, local store (LibSQL). `recall`-style API with score floor + top-K.
- [x] **T8.4** Reference skill **store-info** (RAG: hours, policies) and **orders** (tools: order lookup) — each = prompt fragment + tools + memory schema; ships as example skills, **no domain logic beyond these generic examples** (§13).
- [x] **T8.5** Confirm-before-write round trip: write tool → agent asks via buttons ("Confirm / Cancel") → pending-confirmation state persisted in Memory with expiry → confirm executes once (idempotent), cancel/expiry discards; a later unrelated message doesn't accidentally confirm.
- [ ] **T8.6** Tool-failure path (§10): their API down → caught → honest "team will follow up", `escalate` hook fired.
- [ ] **T8.7** **Spine B** and **Spine C** in e2e with mock Shopify/OpenAPI server.

## Tests introduced (`*.m8.test.ts`, spines in e2e)
- Spine B: touched = {whatsapp, memory, router, skill, rag, llm}; **tools not touched**; answer grounded in retrieved chunk (assert chunk id in prompt).
- Spine C: touched = {whatsapp, memory, router, skill, tools, llm}; **rag not touched**; `getOrder({id:"8842"})` called exactly once with correct args.
- Confirm flow: confirm executes once even if "yes" is delivered twice; expiry; unrelated message; cancel.
- API down (5xx/timeout) → graceful reply + escalate hook.
- Knowledge: chunking edge cases, empty store → falls back to "I don't know" (no hallucinated retrieval), 10k-chunk store latency bound.
- Shopify connector: recorded fixture responses (no live network) → shaped outputs, size-capped.

## Backward testing
M0–M7; **spine A must remain exactly `{whatsapp,memory,router,llm}`** (adding skills/RAG/tools must not leak into the cheap path); tool-selector recall test (M6) re-run with the Shopify toolset; golden tool snapshots (M7) unchanged.

## Exit gate
`pnpm gate 8` green; spines A/B/C green; tag `m8-done`.

## Context notes
Keep the Shopify connector and skills small and clearly "example"; anything that smells like business logic belongs in an app on top (§13).
