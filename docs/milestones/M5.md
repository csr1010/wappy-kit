# M5 — Harness: agent loop, memory, router, skills

**Spec:** §7, §9 (Scenario A), §10 (agent/harness), §2.1 · **Depends on:** M1 · **Tag:** `m5-done` · **Package:** `@wappy/harness`
**Goal:** "hi" works end-to-end on the cheapest path: WhatsApp · Memory · Router · LLM — nothing else touched.

## Read / skip
Read: §7, §9-A, §10 harness list. Skip: RAG/tools internals (M7/M8).

## Tasks
- [ ] **T5.1** `ModelPort` adapter over Vercel AI SDK (`ai`): text, structured output, tool loop, timeout, abort. Tests use `mockModel`; providers (OpenAI/Anthropic/Gemini/Ollama) are just config.
- [ ] **T5.2** LibSQL `Memory` adapter (file + `:memory:`): `load/append/recall`; passes `runMemoryConformance`.
- [ ] **T5.3** LLM `Router`: typed structured output `{intent, skill?, needsRAG, needsTool, escalate, confidence}`; schema validation; **garbage/invalid output → safe default** (general path, no tools, low confidence) rather than crash; one repair attempt; passes `runRouterConformance`.
- [ ] **T5.4** Skill mechanism: prompt fragment + tools + optional memory schema; registration via registry; router sees `availableSkills`.
- [ ] **T5.5** `Agent`: load memory → route → (skill/RAG/tool stubs) → compose → confidence gate → send → persist → trace, each step with a fallback. **Per-contact serialization** (queue per `contactId`) so two rapid messages never interleave turns or double-persist.
- [ ] **T5.6** Model emits `SmartMessage` via structured output; validate; one repair; otherwise degrade to plain text of whatever text exists.
- [ ] **T5.7** Failure paths (§10): model error/timeout/429 → backoff retry → honest "try again shortly"; out-of-scope → honest decline; `escalate` flag surfaced to a handler hook.
- [ ] **T5.8** **Spine A** in `packages/e2e`: real `@wappy/whatsapp` (M3+M4) + real harness + `mockModel` + `mockWhatsAppCloud`. Asserts `tracer.touched() === {whatsapp, memory, router, llm}` and exactly **one** LLM call, **zero** tool/RAG calls.

## Tests introduced (`*.m5.test.ts`, spine in e2e)
- Router decision table (~30 labeled messages via scripted model) + malformed-output cases.
- Agent: idempotent persist (replay same message id → one turn), per-contact ordering under 20 concurrent messages, tool/RAG never touched on greeting.
- Failure injection: model throws, times out, returns invalid SmartMessage, returns empty text.
- Spine A + variants (returning contact with history, model error mid-flow).

## Backward testing
M0–M4 suites; B6 conformance now includes real Memory + Router; spine A uses the *real* M3/M4 layer so any M3/M4 regression surfaces here; contract snapshot unchanged except additive.

## Exit gate
`pnpm gate 5` green; spine A green; tag `m5-done`.

## Context notes
Harness must not import `@wappy/whatsapp`. Wiring lives in e2e (and later the generated `index.ts`). Prompt assembly here is deliberately naive — M6 replaces it with the budgeted assembler; keep it behind one function so the swap is one file.
