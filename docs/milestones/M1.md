# M1 — Core contracts, registry, tracer

**Spec:** §3, §15.1, §6.3 (SmartMessage), §9 (trace) · **Depends on:** M0 · **Tag:** `m1-done`
**Goal:** Freeze the interfaces. After this, parts are built in parallel against them.

## Read / skip
Read: §3, §6.1 (limits only), §9. Skip: §4, §8 internals.

## Tasks
- [x] **T1.1** Write `docs/CONTRACTS.md` (signatures in prose) and ratify in SPEC §16 (+ version bump to 2.1): add **Tracer** and **Skill** to core; note **Knowledge/RAG** lives in harness. Resolves §15.1. *(commit 161559d)*
- [x] **T1.2** Types + runtime schemas (zod or equivalent): `InboundMessage`, `SmartMessage` (with reserved `flow?` slot), `DeliveryResult` (`sent|failed|queued|fellBack` + reason), `Turn`, `RouterDecision` (confidence ∈ [0,1]), `ToolResult`. Export `smartMessageJsonSchema` — the model emits it (§6.3). *(commit 1d10aae)*
- [x] **T1.3** Interfaces: `Agent`, `Router`, `Tool`/`ToolProvider` (allowList, confirmBefore, readOnly), `MessageChannel`, `Memory`, `Skill`, `Model port`, `Clock`, `Logger`. *(commit 144fd1a)*
- [x] **T1.4** `PluginRegistry`: `register(plugin)`, duplicate-name rejection, semver range check of plugin vs core, deterministic ordering, `get<T>(kind)`; config loader for the enabled list. *(commit 99a49b2)*
- [x] **T1.5** `SetupManifest` types (steps + env keys) and aggregation helper (ledger persistence is M2). *(commit 6da852c)*
- [x] **T1.6** `Tracer` interface + in-memory impl: `record(system, event)`; systems = `whatsapp|memory|router|skill|rag|tools|llm`; `touched()` set used by spine tests. *(commit 5546e90)*
- [x] **T1.7** Contract snapshot tool `scripts/contract.mjs` → `contracts/core.api.json`; wire B3 into gate; `pnpm contract:update`. *(commit 5ba262f)*
- [x] **T1.8** testkit fakes + **conformance suites** (B6): `FakeChannel`, `FakeMemory`, `FakeRouter`, `FakeToolProvider` and `run{Channel,Memory,Router,ToolProvider}Conformance(impl)`. *(commit 2040f4f)*

## Tests introduced (`*.m1.test.ts`)
- Schema accept/reject tables (limits from §6.1: 3 buttons, title ≤20, list ≤10 rows, row title ≤24, desc ≤72 — the *schema* allows over-limit input that M4 truncates; document which layer enforces what).
- Registry: dup names, version skew, ordering, unknown kind.
- Conformance suites run against the fakes (proves the suites themselves are sound; negative test: a deliberately broken fake fails them).
- Contract snapshot: baseline check + a planted removal fails.
- Type-level tests (`expectTypeOf`) for the public surface.

## Backward testing
- M0 suites (B1). B9 now has teeth: core has zero plugin imports.
- From here B3 guards the API: any later milestone that edits core must pass `contract check` (additive only).

## Exit gate
`pnpm gate 1` green; `contracts/core.api.json` committed; spec version bumped; tag `m1-done`.

## Context notes
Keep core dependency-light (zod + nothing else). Later sessions should only ever need `CONTRACTS.md`, not core source.
