# M11 — Integration, release hardening, live check

**Spec:** §11, §12 (acceptance + live boundary), §14 (v0.1), §15.2 · **Depends on:** all · **Tag:** `m11-done`, then `v0.1.0`
**Goal:** A stranger can `npm create wappy` and get a working reply. v0.1.0 is publishable.

## Read / skip
Read: §11, §12, §14. Skip: internals.

## Tasks
- [ ] **T11.1** **Fresh-install E2E (B10):** `pnpm pack` every package → install tarballs into an empty temp dir → run the CLI non-interactively → boot → Spine A/B/C against mocks. Node 20/22/24 in CI.
- [ ] **T11.2** Semver/peerDep hygiene: parts declare `@wappy/core` peer range; runtime skew check + CLI warning tested with mismatched versions.
- [ ] **T11.3** Security pass: secret scan of repo + generated projects; dependency audit; license audit (all deps OSS-compatible, §hard-constraint); confirm no telemetry; webhook signature is *not* optional in generated code (test that an unsigned request is rejected in a generated project).
- [ ] **T11.4** Docs: root README (the one-command hook), per-package READMEs, CONTRIBUTING (the milestone loop), SECURITY.md, docs for building an app on top (§13 boundary, interfaces only).
- [ ] **T11.5** Choose and add LICENSE (proposal: MIT or Apache-2.0 — **user decision**); `package.json` metadata, `files`, `exports`, provenance; `npm publish --dry-run` for all packages.
- [ ] **T11.6** Performance/soak: 1k inbound messages (mock) with duplicates + failures → no crash, bounded memory, exactly-once replies; report numbers in PROGRESS.
- [ ] **T11.7** **Human live-verification checklist** (`docs/LIVE_CHECK.md`): Meta app + test number, tunnel, real webhook verification, real "hi" → reply, real duplicate/retry observation, real template outside window, delivery receipts. Cannot be automated (§12 live boundary); record results + date in PROGRESS.
- [ ] **T11.8** Flip repo to **public** (SPEC §15.2; currently private) — *outward-facing, only after explicit user approval*; tag `v0.1.0`; publish to npm only on explicit approval.

## Tests introduced (`*.m11.test.ts`, e2e)
- Fresh-install E2E matrix; skew warnings; unsigned-webhook rejection in generated project; soak test; publish dry-run content assertions (no `fixtures/`, no `.env`, no tests in tarballs; `exports` resolve).

## Backward testing
**Everything:** `pnpm gate 11` runs B1–B10 across all milestones, all fixtures (state v*, whatsapp in/out, openapi goldens, generated projects), all spines, coverage ratchet, and the fresh-install check. This is the release gate.

## Exit gate
`pnpm gate 11` green in CI on Node 20/22/24; LIVE_CHECK signed off by the user; tags `m11-done`, `v0.1.0`.

## Context notes
Publishing/going public are irreversible-ish and outward-facing: never do them without an explicit go-ahead in that session.
