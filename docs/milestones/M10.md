# M10 — Optional backends (Jev router, Mem0, Cognee, Postgres)

**Spec:** §2.1, §7 (Jev + caveat), §15.3, §4.1 (memory/router options) · **Depends on:** M5, M9 · **Tag:** `m10-done`
**Goal:** Prove swappability: every optional backend passes the *same* conformance suites, is opt-in, and adds no mandatory dependency.

## Read / skip
Read: §7, §15.3. Skip: WhatsApp/OpenAPI.

## Tasks
- [ ] **T10.1** **Research first:** verify Jev (TypeSafe AI) real package name/version/API from official docs; write findings to `docs/research/jev.md` (auth needs, hosted vs local, license, latency). Spec §7 says web research was indicative, not authoritative.
- [ ] **T10.2** Decide: if Jev is viable → `JevRouter`; if not/needs proprietary key → keep it clearly opt-in and *also* ship the local "typed-question over your own model" router variant (§7 caveat). Record in Decisions Log.
- [ ] **T10.3** Router adapter(s) behind the existing `Router` contract; lazy-loaded optional peer deps (no import cost or install requirement when unused).
- [ ] **T10.4** Memory adapters: **Postgres**, **Mem0**, **Cognee** (each researched against current docs first); each passes `runMemoryConformance`; none imported unless selected.
- [ ] **T10.5** Wire choices into the M9 interview + generators (env keys, `.env.example`, doctor checks); add `fixtures/generated/m10-*` snapshots.
- [ ] **T10.6** Labeled router dataset (`fixtures/router/decisions.json`, ~50 messages incl. greeting/out-of-scope/complaint/order/hours): every Router impl reports agreement; regressions vs the LLM baseline flagged (threshold, not equality — model-dependent).

## Tests introduced (`*.m10.test.ts`)
- Router conformance + labeled-dataset agreement for each impl using the mock model / recorded backend responses.
- Memory conformance for each adapter: unit-level with mocked clients always; **live-DB variant runs only when `TEST_PG_URL` (etc.) is set** and is skipped otherwise — the gate reports "skipped (no env)" explicitly, never silently passes.
- Lazy-load test: with the optional package *not installed*, core + default config still work (import graph check).
- Doctor: missing key for selected backend → actionable failure.
- Spine A/B/C parametrized over router × memory choices.

## Backward testing
M0–M9 including old generated projects (B7); default config still needs zero optional deps (regression of "local-first" principle §1.3); contract snapshot unchanged (adapters are impls, not contract changes).

## Exit gate
`pnpm gate 10` green (live-DB tests skipped-with-notice unless env provided); tag `m10-done`.

## Context notes
Each adapter is isolated in its own folder + dependency; a session should only ever open one.
