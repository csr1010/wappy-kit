# PROGRESS — handoff log (READ FIRST on session resume)

Rewrite the **Current handoff** block at the end of every session. Keep it under 40 lines. Append to the Decision & change log; never delete entries.

## Current handoff

- **Date:** 2026-09-21
- **Milestone / task:** M2 DONE (T2.1-T2.9). Tag `m2-done`. M3 not started.
- **Last green gate:** `pnpm gate 2` PASS (lint, typecheck, build, tests m0..m2, B2 immutability, B3 contract snapshot, B8 coverage ratchet, 100% coverage on the new `state/` module). Code-reviewed — two rounds found and fixed a real bug (step identity collision when two parts share a step id; fixed by keying everything on `stepKey(part,id)` — see commits a8c35f5/ba64dd6) — then a third pass came back clean. Manual use-case sanity check: a full install-run simulation (fresh project → locked run with two same-step-id parts → step failure → resume without re-running the done step → drift detection clean/modified → version-skew warning → hand-corrupted file → offer not throw → reset(all)) — see session transcript.
- **Repo:** https://github.com/csr1010/wappy-kit (private) per SPEC, but **this session's sandbox still has no `origin` remote and no GitHub credentials configured** — asked the user for a remote URL + push-capable token after M1; no reply received yet. All commits through `m2-done` exist only locally in this container.
- **Next 3 actions:**
  1. Once a remote/token is provided: `git remote add origin <url>`, push `main` + tags `m1-done`/`m2-done`.
  2. `pnpm ctx M3`, then T3.1 (WhatsApp inbound + reliability base, SPEC §6.2, package `@wappy/whatsapp`).
  3. Bump CI step to `pnpm gate 3` when M3 lands (already at `pnpm gate 2` for M2).
- **Open decisions:** license (MIT vs Apache-2.0), tunnel provider for `wappy dev`, Shopify API flavor (Admin GraphQL vs REST) - see milestone files M11/M9/M8. Still open: git push destination/credentials for this build (asked user 2026-09-21, still awaiting reply as of M2).
- **Gotchas:** pnpm here is v12.5.1 (no `-s`). TS 6 has no default `types`: packages using node APIs need `"types": ["node"]` in tsconfig (testkit/e2e/core now do — core needed it for `state/`'s fs/crypto access). testkit API is documented in the header of packages/testkit/src/index.ts - do not open its internals. Arch guard + ratchet tests live in packages/e2e (scripts/ratchet.mjs, coverage-baseline.json; `pnpm ratchet` only raises the baseline). `gh` is not installed in this sandbox at all (not just off PATH). Repo stays private until M11. **Sandbox quirk (fix once per fresh container):** `turbo run build/typecheck` can fail 100% of the time with `Exec format error (os error 8)` because pnpm's self-managed binary at `~/.local/share/pnpm/.tools/pnpm/<ver>/node_modules/pnpm/pnpm` is a shebang-less shell placeholder until its own `install.js` runs; fix: `node ~/.local/share/pnpm/.tools/pnpm/<ver>/node_modules/pnpm/install.js`. Core's `typecheck` script points at `tsconfig.typecheck.json` (includes `*.test.ts`) so `expectTypeOf`/type-level tests actually get checked; other packages still exclude tests from typecheck (M0 pattern) — adopt the same fix if/when they grow type-level tests. **State module (`packages/core/src/state/`) design notes for future milestones:** `stepKey(part,id)` is the ONLY correct way to look up/upsert a step — `aggregateSetupManifests` dedupes step ids per-part, so two parts can legitimately share a step id. Core owns zero `@wappy/*` deps, so its own tests use a plain `mkdtemp` helper instead of testkit's `tmpProject`, to avoid a testkit<->core dependency cycle (testkit depends on core). `migrate()` has no migration-chain loop yet (v1 is the only version) — add it back, with a real transform function + `fixtures/state/v2.json`, the moment schema v2 exists (see the doc comment on `migrate()`). Every milestone now ends with a code review pass + a manual use-case sanity check + a push to `main` (explicit user instruction, see CLAUDE.md step 4 note).

## Decision & change log

- 2026-09-20: Spec saved to docs/SPEC.md; milestones M0–M11 defined; backward-testing layers B1–B10 defined (see MILESTONES.md).
- 2026-09-20: Proposed spec additions, to be ratified in M1 T1.1 (record in SPEC §16 then): (a) `Tracer` added to core contracts — needed for §9 "trace" and spine tests; (b) `Skill` added to core contracts — §13/§17 treat it as a core interface but §3 omits it; (c) RAG/`Knowledge` module lives in harness — §9-B needs it but no part owns it.
