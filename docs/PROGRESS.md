# PROGRESS — handoff log (READ FIRST on session resume)

Rewrite the **Current handoff** block at the end of every session. Keep it under 40 lines. Append to the Decision & change log; never delete entries.

## Current handoff

- **Date:** 2026-09-21
- **Milestone / task:** M3 DONE (T3.1-T3.9). Tag `m3-done`. M4 not started.
- **Last green gate:** `pnpm gate 3` PASS (lint, typecheck, build, tests m0..m3, B2 immutability, B3 contract snapshot, B8 coverage ratchet, 100% coverage on `@wappy/whatsapp`). Code-reviewed — **five rounds**, each finding and fixing a real bug: (1) interactive-reply id and location lat/long silently dropped (core schema gained `InboundMessage.selectionId` + `InboundMedia.latitude/longitude`, additive); (2) `createMemorySeenStore` grew unbounded forever; (3) a non-streaming media-download fallback buffered before checking the size cap, and the fix to (2) made dedup O(n)/call; (4) marking a status "seen" before calling `onStatus` meant a throwing handler lost that status permanently on retry; (5) the fix to (4) reintroduced a concurrent-duplicate race, and a WhatsApp Flow reply (`interactive.type === "nfm_reply"`) fell through to a content-less message instead of being flagged unsupported — both fixed together (`SeenStore.checkAndSet`+`forget()` instead of `has()`-then-set; interactive branch now checks for a recognized title before returning). Sixth review pass came back clean. Manual use-case sanity check: full reliability-engine walkthrough (handshake → signature verify incl. tamper rejection → duplicate webhook dedup → button reply with selectionId → location with lat/long → status lifecycle sent/delivered/read with duplicate-retry dedup → failed-status error code → unsupported type flagged → garbage/null webhooks don't crash → real send() round-trip) — see session transcript.
- **Repo:** https://github.com/csr1010/wappy-kit (private) per SPEC, but **this session's sandbox still has no `origin` remote and no GitHub credentials configured** — asked the user for a remote URL + push-capable token after M1; no reply received yet as of M3. All commits through `m3-done` exist only locally in this container.
- **Next 3 actions:**
  1. Once a remote/token is provided: `git remote add origin <url>`, push `main` + tags `m1-done`/`m2-done`/`m3-done`.
  2. `pnpm ctx M4`, then T4.1 (WhatsApp smart send + fallback, SPEC §6.1/§6.3, package `@wappy/whatsapp`) — this replaces `channel.ts`'s current text-only `send()` with real SmartMessage rendering (buttons/list/cta/media) + capability-aware fallback.
  3. Bump CI step to `pnpm gate 4` when M4 lands (already at `pnpm gate 3` for M3).
- **Open decisions:** license (MIT vs Apache-2.0), tunnel provider for `wappy dev`, Shopify API flavor (Admin GraphQL vs REST) - see milestone files M11/M9/M8. Still open: git push destination/credentials for this build (asked user 2026-09-21, still awaiting reply as of M3).
- **Gotchas:** pnpm here is v12.5.1 (no `-s`). TS 6 has no default `types`: packages using node APIs need `"types": ["node"]` in tsconfig (testkit/e2e/core/whatsapp now do). testkit API is documented in the header of packages/testkit/src/index.ts - do not open its internals. Arch guard + ratchet tests live in packages/e2e (scripts/ratchet.mjs, coverage-baseline.json; `pnpm ratchet` only raises the baseline). `gh` is not installed in this sandbox at all (not just off PATH). Repo stays private until M11. **Sandbox quirk (fix once per fresh container):** `turbo run build/typecheck` can fail 100% of the time with `Exec format error (os error 8)` because pnpm's self-managed binary at `~/.local/share/pnpm/.tools/pnpm/<ver>/node_modules/pnpm/pnpm` is a shebang-less shell placeholder until its own `install.js` runs; fix: `node ~/.local/share/pnpm/.tools/pnpm/<ver>/node_modules/pnpm/install.js`. Core's `typecheck` script points at `tsconfig.typecheck.json` (includes `*.test.ts`); other packages still exclude tests from typecheck (M0 pattern) — adopt if/when they grow type-level tests. **State module notes (M2):** `stepKey(part,id)` is the ONLY correct way to look up/upsert a step. Core owns zero `@wappy/*` deps (tests use a plain `mkdtemp` helper, not testkit's `tmpProject`, to avoid a cycle). `migrate()` has no migration-chain loop yet (v1 is the only version) — add it back with `fixtures/state/v2.json` the moment schema v2 exists. **WhatsApp module notes (M3, `packages/whatsapp/src/`):** `createWhatsAppChannel`'s `send()` is deliberately text-only for now (M4 replaces it). `SeenStore` is memory-only; a persistent (LibSQL) impl is deferred to M5/M10 when that dependency actually exists. Status dedup key is `status:<messageId>:<status>` (not messageId alone — a message legitimately passes through several distinct statuses). Any caller that does failable work after a `checkAndSet` claim MUST `forget()` the key on failure or that id is lost for the whole TTL — see channel.ts's status loop for the pattern. `fixtures/whatsapp/inbound/*.json` are synthesized from Meta's docs, NOT verified against live traffic yet (flagged in the fixtures README). Every milestone now ends with a code review pass (repeated until clean) + a manual use-case sanity check + a push to `main` (explicit user instruction, see CLAUDE.md step 4 note).

## Decision & change log

- 2026-09-20: Spec saved to docs/SPEC.md; milestones M0–M11 defined; backward-testing layers B1–B10 defined (see MILESTONES.md).
- 2026-09-20: Proposed spec additions, to be ratified in M1 T1.1 (record in SPEC §16 then): (a) `Tracer` added to core contracts — needed for §9 "trace" and spine tests; (b) `Skill` added to core contracts — §13/§17 treat it as a core interface but §3 omits it; (c) RAG/`Knowledge` module lives in harness — §9-B needs it but no part owns it.
